{
  "name": "infernode",
  "version": "0.0.0",
  "description": "flamegraph tool for node.js",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "clean": "rm -rf ./dist",
    "copy-assets": "cp -R ./src/assets ./dist/",
    "build": "tsc && npm run copy-assets",
    "test": "jest",
    "dev": "mkdir -p ./dist && touch ./dist/index.js && concurrently \"npx tsc --watch\" \"nodemon dist/index.js\""
  },
  "nodemonConfig": {
    "watch": [
      "./src/assets",
      "./dist/index.js"
    ],
    "ignore": [
      "./dist/bundle.js"
    ],
    "events": {
      "restart": "mkdir -p ./dist && npm run copy-assets && echo 'copied assets'"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oslabs-beta/infernode.git"
  },
  "keywords": [
    "flamegraph",
    "perf",
    "node.js"
  ],
  "author": "infernode dev team",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oslabs-beta/infernode/issues"
  },
  "homepage": "https://github.com/oslabs-beta/infernode#readme",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.7.2",
    "concurrently": "^7.3.0",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "tslint": "^6.1.3",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  }
}
